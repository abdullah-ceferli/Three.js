SPACETIMEDB_HOST=http://127.0.0.1:3000
SPACETIMEDB_DATABASE=wildwood-survival
# Use the token belonging to the identity that publishes the module.
SPACETIMEDB_TOKEN=
# Keep this stable across restarts so browser sessions remain valid.
WILDWOOD_SESSION_SECRET=replace-with-at-least-32-random-characters
